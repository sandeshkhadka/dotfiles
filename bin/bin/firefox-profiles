#!/bin/bash

profile=$(grep Name /home/pqr/.mozilla/firefox/profiles.ini | awk -F "=" '{print $2}' | rofi -dmenu -p "Profile: ")
if [ -z $profile ]
then
  exit
fi
# echo "$profile"
# firefox-developer-edition -P $profile
firefox -P $profile &
